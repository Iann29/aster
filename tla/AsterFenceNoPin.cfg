\* NEGATIVE config — Lemma R's check-then-use race (theorem Repair
\* G-RETENTION): the fence checks coverage g <= s but does NOT hold the
\* retention row lock through append, so GC may prune events of (s, h]
\* mid-validation. TLC must find an I2_NoValidationAgainstPruned violation.
SPECIFICATION Spec
CONSTANTS
    Committers = {c1, c2}
    Keys = {k1, k2}
    MaxPos = 4
    MaxEpoch = 3
    None = None
    EpochReuse = FALSE
    RetentionPinned = FALSE
SYMMETRY Symm
INVARIANTS
    TypeOK
    I1_UniquePosition
    I2_NoValidationAgainstPruned
    I3_EpochBlockOrder
    I4_NoWriteSkew
    I4a_NoStaleValidatedRead
