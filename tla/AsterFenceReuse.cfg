\* NEGATIVE config — the F1 violation (referee report): the lease authority
\* reinstalls an old epoch on failback A->B->A instead of bumping. The V2
\* equality check still passes, so TLC must find an I3_EpochBlockOrder
\* violation: a stale-epoch commit ordered after a newer-epoch commit,
\* breaking Lemma 3.11's exhaustive case analysis. Proves the model has teeth.
SPECIFICATION Spec
CONSTANTS
    Committers = {c1, c2}
    Keys = {k1, k2}
    MaxPos = 4
    MaxEpoch = 3
    None = None
    EpochReuse = TRUE
    RetentionPinned = TRUE
SYMMETRY Symm
INVARIANTS
    TypeOK
    I1_UniquePosition
    I2_NoValidationAgainstPruned
    I3_EpochBlockOrder
    I4_NoWriteSkew
    I4a_NoStaleValidatedRead
